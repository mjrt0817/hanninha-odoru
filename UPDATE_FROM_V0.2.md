# Ver.0.2 → Ver.0.3 更新内容

## 今回の主な変更

- 写真から切り抜いた32枚のカード画像を廃止しました。
- カードを「データ + 共通HTML/CSSテンプレート + SVGベクターイラスト」で描画する方式に変更しました。
- 端末ごとのカード画像ダウンロードが不要になりました。
- カード追加時は `lib/cards.ts` に定義を追加するだけで、基本レイアウトをそのまま利用できます。
- `/cards` にデジタルカード一覧画面を追加しました。
- PCテストモードも新しいデジタルカード表示へ切り替えました。
- Ver.0.2までのカード画像キャッシュはService Workerが削除します。

## Supabaseについて

Ver.0.3ではDB構造の変更はありません。
Ver.0.2の `upgrade_v0_2.sql` を実行済みであれば、追加SQLは不要です。

## Vercel更新

既存GitHubリポジトリをこのVer.0.3の内容で上書きしてPushしてください。
Vercelの環境変数も変更不要です。

## 確認URL

- `/cards` : 32枚の新デジタルカード一覧
- `/test` : PCテストモード
