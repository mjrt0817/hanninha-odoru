# Ver.0.1 → Ver.0.2 更新手順

既にVercelでVer.0.1が動いている場合は、次の順で更新してください。

1. Supabase → SQL Editor → New query
2. `supabase/upgrade_v0_2.sql` の全文を貼り付けて Run
3. GitHubの既存 `hannin-family-app` リポジトリを、このVer.0.2の内容で更新
4. GitHubへの反映後、Vercelが自動Deployするのを待つ
5. 新しいVercel URLを開く
6. トップの「PCテストモード（最大4人）」で動作確認

## 最初に確認すること

- 4人テストを開始できる
- 4人分の手札がPC上に並ぶ
- 第一発見者のカードだけ黄色枠で操作できる
- 第一発見者を押すと事件入力が出る
- 「この事件で始める」で第一発見者が消え、次の人へ手番が移る

## 実端末テスト

通常ルームを3人以上で開始し、第一発見者の端末でカードをタップしてください。
事件入力後、全端末に事件が表示され、次の人へ手番が移れば成功です。

## 画像が以前のままの場合

Ver.0.2ではService Workerのキャッシュ名を変更し、旧画像キャッシュを自動削除します。
それでも残る場合は、一度ページを閉じて再度開くか、ブラウザで再読み込みしてください。
